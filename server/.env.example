# Copy this file to server/.env and fill in secrets. Never commit .env.
# The browser posts to send-callback.php in the site root; that file loads this folder (vendor + .env).

MAIL_MAILER=smtp
MAIL_HOST=smtppro.zoho.com
MAIL_PORT=465
MAIL_USERNAME=anchor@kloud.com.bd
# Use quotes if the password contains $ or special characters.
MAIL_PASSWORD="bmkg$Om61"
MAIL_ENCRYPTION=ssl
MAIL_FROM_ADDRESS=anchor@kloud.com.bd
MAIL_FROM_NAME="Kloud Website"

# Comma-separated list of addresses that receive each submission
CALLBACK_RECIPIENTS=arman.alawaf@gmail.com,arman.p2c@gmail.com

# If the website runs on another host (e.g. GitHub Pages) POSTing to this script, list allowed Origins:
# CALLBACK_ALLOWED_ORIGINS=https://kloud.com.bd,https://www.kloud.com.bd
